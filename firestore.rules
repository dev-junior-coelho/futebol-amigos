rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
      // Regras para jogadores (players)      || (request.resource.data.keys().hasAll(['name', 'position']) 
          && request.resource.data.keys().hasAny(['peDominante', 'idade', 'assists', 'statusPagamento', 'createdAt', 'updatedAt']));
    }
  }
}
